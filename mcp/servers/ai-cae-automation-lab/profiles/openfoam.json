{
  "solver": "openfoam",
  "maturity": "bridge-preview",
  "capabilities": [
    "check local/WSL commands",
    "run selected case commands",
    "capture logs"
  ],
  "rules": [
    "Resolve paths through setup discovery, environment variables, or private local config.",
    "Create a run record before launching tools.",
    "Only claim success from logs and evidence artifacts."
  ]
}
